name: fortest
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter_naver_map: ^1.0.0
  english_words: ^4.0.0
  flutter:
    sdk: flutter
  geolocator: ^8.0.3
  go_router: ^12.0.0
  image: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

